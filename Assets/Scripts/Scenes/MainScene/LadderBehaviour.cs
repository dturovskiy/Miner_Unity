using UnityEngine;

public class LadderBehaviour : MonoBehaviour
{
    // Logic moved to LadderZoneDetector on the Hero GameObject itself
    // to correctly count multiple ladder zones and prevent state drops.
}
